---
import RoleTable from "./RoleTable.astro";
import type { AdminPermission, AdminRole } from "../../lib/auth/types";

interface Props {
	roles: AdminRole[];
	permissions: AdminPermission[];
}

const { roles } = Astro.props;
---

<RoleTable roles={roles} />
