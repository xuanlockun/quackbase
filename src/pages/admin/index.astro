---
export const prerender = false;

import { getDefaultAdminPath } from "../../lib/rbac/policies";

const session = Astro.locals.adminSession;
return Astro.redirect(getDefaultAdminPath(session));
---
